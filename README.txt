ICU 溝通平板 V2.9｜一般多語＋長者模式＋互動定向模式｜修正版

本版修正：
- 修正上一版互動題目造成 JavaScript 解析錯誤，導致網頁卡在「請先啟用聲音」畫面的問題。
- 聲音啟用按鈕改成更穩定寫法，不再依賴瀏覽器自動把 id 變成全域變數。
- 保留一般多語模式、長者模式、互動定向模式。
- 保留慢速語音、抽痰 SVG 圖示、「我口渴」、報時定位。
- 互動定向模式僅作為注意力與定向互動輔助，不判定譫妄陽性或陰性；正式評估仍依單位 CAM-ICU／ICDSC 流程。

GitHub 使用方式：
把 ZIP 解壓縮後，用 index.html 覆蓋原本 repository 的 index.html。網址不用換。
